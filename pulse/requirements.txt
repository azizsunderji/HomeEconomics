# Pulse — Conversation Monitor for Home Economics
# Core
anthropic>=0.40.0
httpx>=0.27.0
duckdb>=0.10.0

# RSS/Atom parsing
feedparser>=6.0
googlenewsdecoder>=0.1.7

# Data lake queries (DuckDB .df() needs pandas/numpy)
pandas>=2.0
numpy>=1.24

# Reddit collector
requests>=2.31

# Utilities
jinja2>=3.1

# Browser automation (Browserbase connect_over_cdp; no local chromium needed)
# Retained for any other scripts that may shell into Playwright; the
# front-pages pipeline no longer uses it.
playwright>=1.40
browserbase>=0.5.0

# Image processing for stylized front-page composites
Pillow>=10.0
PyMuPDF>=1.24
pdfplumber>=0.11
numpy>=1.24
